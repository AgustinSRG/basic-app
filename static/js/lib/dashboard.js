$(function () {

    /*$(".grid-stack").gridstack( {staticGrid: true} );


    /*$(window).on('resize', function(){
        widgets = $('.grid-stack').children("div");

        for (var i = 0; i < widgets.length; i++) {
            var grafico = widgets[i].childNodes[0].childNodes[1].childNodes[0].__chartist__;
            panel = widgets[i].childNodes[0];
            var altura = panel.offsetHeight - 50;

            //grafico.update(0, {height: altura });
        }

    });*/

    //Workspace.grid = $(".grid-stack").data("gridstack");

    //Workspace.loadGrid();

});
